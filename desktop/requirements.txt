fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
httpx>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.6,<3.0

# Catatan: sengaja TIDAK pakai "uvicorn[standard]". Extras itu menarik
# uvloop & httptools yang sering gagal build di Termux/Android (dan uvloop
# tidak didukung di Windows sama sekali). "websockets" tetap perlu dipasang
# terpisah karena itu yang dipakai uvicorn untuk endpoint WebSocket /mcp.
